// 根目录 build.gradle
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // 确保使用支持 Java 17 的版本
        classpath 'com.android.tools.build:gradle:8.1.0'
    }
}

// 删掉了原本引起冲突的 allprojects { repositories { ... } }