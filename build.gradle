// 根目录 build.gradle
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // 在这里统一管理版本
        classpath 'com.android.tools.build:gradle:8.1.0'
    }
}

// 确保这里没有 allprojects 块，之前已经删过了
